// @afr/ingest — HTTP batch endpoint → ClickHouse async inserts (milestone 4).
const PORT = Number(process.env.AFR_INGEST_PORT ?? 4000);
console.log(`[ingest] skeleton — real server lands in milestone 4 (port ${PORT})`);
